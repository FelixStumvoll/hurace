<UserControl x:Class="Hurace.RaceControl.Views.Controls.RaceDisplayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Hurace.RaceControl.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:RaceDisplayViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Content="Aktueller Rennläufer" Command="{Binding OpenCurrentSkierWindowCommand}"/>

        <!-- <subViews:CurrentSkierView Grid.Row="0" /> -->
        <!-- <subViews:SplitTimeView Grid.Row="1" /> -->
    </Grid>
</UserControl>