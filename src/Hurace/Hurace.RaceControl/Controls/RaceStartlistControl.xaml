<UserControl x:Class="Hurace.RaceControl.Controls.RaceStartlistControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Hurace.RaceControl.Controls"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:dto="clr-namespace:Hurace.Core.Dto;assembly=Hurace.Core.Dto"
             xmlns:vm="clr-namespace:Hurace.RaceControl.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance vm:RaceItemStartListViewModel}">

    <UserControl.Resources>
        <DataTemplate x:Key="SkierTemplate" DataType="{x:Type dto:Skier}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Country.CountryCode}" />
                <TextBlock Margin="5,0" Text="-" />
                <TextBlock Text="{Binding FirstName}" Margin="0,0,5,0" />
                <TextBlock Text="{Binding LastName}" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Column="0" Grid.Row="0" Text="Alle Rennläufer:" />
        <TextBlock Grid.Column="1" Grid.Row="0" Margin="5,0,0,0" Text="Eingetragene Rennläufer:" />
        <!-- Search -->
        <TextBox Grid.Column="0" Grid.Row="1" x:Name="AvailableSearch" Margin="0,5,5,5"
                 Text="{Binding AvailableSearchText}">
            <i:Interaction.Triggers>
                <i:PropertyChangedTrigger Binding="{Binding ElementName=AvailableSearch, Path=Text}">
                    <i:InvokeCommandAction CommandParameter="{Binding ElementName=AvailableSearch, Path=Text}"
                                           Command="{Binding AvailableSearchTextChange}" />
                </i:PropertyChangedTrigger>
            </i:Interaction.Triggers>
        </TextBox>
        <TextBox Grid.Column="1" Grid.Row="1" x:Name="SelectedSearch" Margin="5,5,0,5"
                 Text="{Binding SelectedSearchText}">
            <i:Interaction.Triggers>
                <i:PropertyChangedTrigger Binding="{Binding ElementName=SelectedSearch, Path=Text}">
                    <i:InvokeCommandAction CommandParameter="{Binding ElementName=SelectedSearch, Path=Text}"
                                           Command="{Binding SelectedSearchTextChange}" />
                </i:PropertyChangedTrigger>
            </i:Interaction.Triggers>
        </TextBox>
        <!-- Available Skiers -->
        <ListBox Grid.Column="0" Grid.Row="2" Grid.RowSpan="2" x:Name="AvailableSkiers" Margin="0,0,5,0"
                 ItemsSource="{Binding AvailableSkiers}"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel LastChildFill="False">
                        <ContentPresenter ContentTemplate="{StaticResource SkierTemplate}" />
                        <Button DockPanel.Dock="Right" Content="+" Width="35" CommandParameter="{Binding Id}"
                                Command="{Binding ElementName=AvailableSkiers, Path=DataContext.AddSkier}" />
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <!-- Selected Skier -->
        <ListBox Grid.Column="1" Grid.Row="2" x:Name="SelectedSkiers" Margin="5,0,0,0"
                 ItemsSource="{Binding SelectedSkiers}"
                 SelectedItem="{Binding SelectedStartList}"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel LastChildFill="False">
                        <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                            <TextBlock Margin="0,0,10,0" FontWeight="Bold" Text="{Binding StartNumber}" />
                            <ContentPresenter ContentTemplate="{StaticResource SkierTemplate}" Content="{Binding Skier}" />
                        </StackPanel>
                        <Button DockPanel.Dock="Right" Content="-" Width="35" CommandParameter="{Binding SkierId}"
                                Command="{Binding ElementName=SelectedSkiers, Path=DataContext.RemoveSkier}" />
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Column="1" Grid.Row="3" Margin="5,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="0,0,5,0" Content="↑" Command="{Binding StartListUp}" />
            <Button Grid.Column="1" Margin="5,0,0,0" Content="↓" Command="{Binding StartListDown}" />
        </Grid>

    </Grid>
</UserControl>